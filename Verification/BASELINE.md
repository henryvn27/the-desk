# Electron rebuild baseline — 2026-09-05

Tracker: CS-2165. Branch: codex/desk-v1-electron. Base: c4bdbdfbe5c2fc82a80d4957f7c23bc1d0bca086. Canonical checkout: /Users/henry/Developer/TheDesk/repo.

The saved old local paths were absent. Remote main contained the Swift prototype. The rebuild branch removes all 72 tracked legacy implementation/configuration/test/visual-reference files following the user's explicit full-rebuild correction. Original history is retained. No installed prototype or personal academic data was deleted.

Passed: dependency installation with valid peer resolution; strict TypeScript; ESLint; four domain/planner scenarios; Vite and Electron main/preload production build; unsigned macOS arm64 directory packaging.

Passed development runtime smoke: class and task capture, Home Next, session start, pause/restart/resume, session-aware Lens freehand selection and dismissal, explicit completion, completed-task restart. `npm run test:desktop` reproduces this with an isolated temporary database and records video/screenshots under ignored `artifacts/smoke`.

The smoke is not the full V1 vertical: no AI, voice, external-screen interpretation, real integration, cloud sync, or installed Windows acceptance. Initial Lens is selection only. Package evidence is unsigned development packaging, not public distribution or updater acceptance.

Outstanding first-slice gaps include broader natural-input interpretation, planner preferences/learning, live screen capture and actual Lens assistance, resource proof, and fuller graph. The completion map remains conservative; requirement groups must not be marked verified because one subpart passes.

Installed development proof: `/Users/henry/Applications/The Desk V1.app` (unsigned arm64) passes the same smoke using `DESK_EXECUTABLE` and an isolated temporary database. The live script records automated interaction video. Lens one-shot screen capture code has been added after the initial baseline; capture permission/granted-screen behavior remains unverified. No capture is performed until its explicit button is pressed.

Latest checkpoint: `npm run check` passes 27 tests including capture timezone/uncertainty, schema-1-to-2 migration, and provider error/usage contracts. Pasted-text capture is live-tested through review, explicit date-time confirmation and persisted provenance. The provider adapter uses injected fetch in tests; it has not produced a real API response. Lens UI's missing-key fallback passes live. Native screen capture, provider authentication, overlay grounding, voice and Windows remain unverified. Developer model cost estimates come from the provider's actual usage when present; no production economics measurement is claimed.

Task correction checkpoint: 28 tests, typecheck, lint and production build pass. The updated unsigned installed macOS bundle passes task-edit smoke: confirmed-deadline changes are rejected without approval; approved corrections survive restart and retain original capture provenance. Capture is paste-first with a separate manual path; source details collapse so actions fit in the inspected window. No full planner or Windows acceptance is implied.

Session review checkpoint: 29 tests plus typecheck/lint/build pass. Installed unsigned macOS smoke confirms one-click acknowledgment, optional work notes and remaining-time updates, restart persistence, and Library study history. Original measured duration is retained; review cannot overwrite estimates after a newer session or put remaining time on completed work. Screenshots and operated video captured. This is partial SESSION evidence: no automatic detection or mastery/attempt/mistake/preparedness learning is claimed.

Planning preferences checkpoint: 31 tests pass with schema-3 settings migration, preference persistence, invalid-window rejection, off-days and local DST boundaries. Installed macOS smoke verifies saved study hours/day selection/buffer survive restart and off-days yield no automatic blocks. The current planner remains same-day and deadline-first; grade impact, learned estimates, spacing, locked blocks and multi-day planning are incomplete.

Cross-platform automated checkpoint: GitHub Actions run https://github.com/henryvn27/the-desk/actions/runs/33997130565 succeeded on source commit effd09b05eef6132144abc09f9925f0581431562. Both windows-latest and macos-latest completed npm ci, npm run check and npm run package. This proves hosted build/test/directory-package validation, not Windows installed UI, native Lens, shortcuts, permission handling, signed distribution or updater acceptance.

Weekly planning checkpoint: 32 tests and installed macOS smoke pass. A seven-local-day horizon carries only remaining task minutes between available days, preserves deadlines and buffer, and reports work still outside the horizon. Plan displays real dates and saved preferences. This is computed planning, not persisted commitments, grade-aware priorities, spacing or lock support. Prior Windows CI covers effd09b; this weekly-planning change has not yet been run on Windows.

Source checkpoint: 33 tests plus typecheck/lint/build pass. Schema 4 adds first-class text sources with normalized class/task many-to-many links. Installed macOS smoke verifies capture, linked assignment, content search, rendered source reader and exact text/link persistence after restart. Linked text is also available in the main session panel; that panel source flow has not yet been separately exercised. File/web imports, source editing/undo, provider retrieval and full academic graph remain unfinished.

Canvas checkpoint: 39 tests, typecheck/lint/build, development and installed Canvas smoke pass. The installed full desktop regression passed on retry after one extra Lens SVG-path assertion failure; QA-LENS-002 remains open. Canvas uses a lazy-loaded Excalidraw adapter and schema-5 revision-checked records. Verified drawing scope is ink/arrow/rectangle/undo-redo/close/restart, not complete toolbar acceptance. Scoped dependency overrides clear npm audit; React peer warnings remain documented. License resource was copied into the unsigned installed/development bundle after packaging and extraResources now preserves it in subsequent builds.

Canvas CI and export checkpoint: run https://github.com/henryvn27/the-desk/actions/runs/33998283212 passed checks/build/package on Windows and macOS for 48d46198d32b5b756e17edeb4cc0b80493787d4e. A subsequent PNG export change passes typecheck/lint/build and installed Canvas smoke including cancellation and actual PNG-file inspection. Native save-dialog results were injected; manual dialog interaction is not claimed. PNG export is not yet checked on Windows.

Canvas text/image checkpoint: the installed 0c5a45d application passes the extended Canvas smoke with text and an imported PNG, exact scene/binary data after restart, and Excalifont loaded under the local-only CSP. Screenshot and operated video captured. The test injects only the File System Access picker result; native image-picker interaction remains unverified. Initial Playwright filechooser waiting timed out because this picker uses a different API. Upstream CDN font fallbacks are blocked by CSP; no remote font permission was added. This checkpoint changes test/evidence only, not app implementation.

Canvas native-close checkpoint: 39 tests, typecheck/lint/build, packaging, development runtime and installed macOS Canvas smoke pass. Native Quit flushes an edit while the 500 ms debounce is deliberately held in the test; restarting proves the ellipse persisted. A simulated concurrent revision makes native close fail safely, leaving the editor open and the stored scene unchanged. Screenshot and operated videos captured. This installed package auto-selected the local Apple Development signing identity; notarization was skipped. It is a development-signed arm64 build, not release-distribution evidence. Forced termination and dedicated conflict-recovery controls remain unfinished; Windows has not yet verified this change.

Cross-platform Canvas close checkpoint: GitHub Actions run https://github.com/henryvn27/the-desk/actions/runs/33999254685 succeeded on 2b4468806fdb2d65558c9ec0e32793099fb8f6b4. macOS and Windows completed npm ci/check/package. This includes PNG export and native-close implementation, but hosted CI does not exercise native UI or the subsequent recovery-copy change.

Canvas conflict-recovery checkpoint: 39 tests plus typecheck/lint/build and development/installed macOS Canvas smoke pass. A stale editor can save its unsaved drawing as a separate recovery copy, close, and open that copy from Library; the original remains unchanged and the copy survives restart. Embedded image data remains present. Excalidraw normalizes a new shape binding field on first open (null to empty array), so restart comparison uses the post-save scene. Screenshot and operated video captured. This package disables signing auto-discovery and is not notarized. Forced-termination journaling, automated merge, and interactive Windows recovery remain unverified/unimplemented.

Canvas source-link checkpoint: 39 tests, typecheck/lint/build, packaging and installed macOS smoke pass. The Sources panel links existing Library text to Canvas and renders the original passage beside the drawing. Links survive restart and recovery-copy creation; unlinking preserves the source. Invalid source references fail transactionally without advancing the Canvas revision. Screenshot and operated video captured. The current source type is pasted text; PDF/web ingestion, other Canvas tools and interactive Windows evidence remain incomplete.

Cross-platform source-link checkpoint: run https://github.com/henryvn27/the-desk/actions/runs/33999701817 passed macOS and Windows checks/build/package on e27e01acfdf416e22f54664c4df847cf22298443, including recovery copies and source links. Hosted checks do not establish interactive Windows behavior.

Canvas ink-preset checkpoint: 39 tests, typecheck/lint/build, packaging and installed macOS smoke pass. Explicit Pen and Highlighter controls produce opaque dark width-2 ink and translucent yellow width-8 ink respectively. Saved style properties and scene survive undo/redo and restart. The installed screenshot shows an existing arrow visible beneath the highlighter; operated video captured. These presets do not establish complete eraser/lasso/selection/pressure/tilt/tool acceptance.

Canvas math checkpoint: 41 tests, typecheck/lint/build, development Canvas regression, packaging and installed math smoke pass. Local MathJax 4.1.3 renders base/AMS LaTeX; bounded invalid/external-content input is rejected. Math PNG and editable normalized LaTeX persist together. Installed proof covers insertion, toolbar undo/redo, restart, editing existing LaTeX, second restart, and PNG export with an injected save-dialog result. Quadratic/integral screenshots and the exported integral were visually inspected; operated videos captured. QA-CANVAS-001 retains unverified math keyboard undo. Windows math interaction and the complete license/Canvas/release matrix remain open.

Canvas keyboard checkpoint: 41 tests, typecheck/lint/build and installed math smoke pass after routing undo/redo accelerators past Electron native Edit-menu interception. Intermediate assertions prove the equation disappears on Cmd+Z and returns on Cmd+Shift+Z; text-field undo stays inside the Math dialog. Prior generic drawing smoke pressed undo/redo but compared only the final scene, so it did not independently prove the intermediate undo. Native Command-Q closed the isolated installed test app via CUA; Playwright synthetic Cmd+Q did not establish that path. QA-CANVAS-001 is resolved for the observed macOS failure. Direct native Edit-menu actions and Windows runtime remain unverified.

Cross-platform math checkpoint: run https://github.com/henryvn27/the-desk/actions/runs/34000401116 passed checks/build/package at 80a6febbbf786a9e6ab5038830d5c894bbf8acd7. It does not cover the later keyboard routing change or interactive Windows math acceptance.


Canvas native-menu checkpoint: 41 tests, typecheck/lint/build, unsigned directory packaging and installed macOS math smoke pass. Menu callbacks now use the window supplied by Electron instead of querying transient global focus; a diagnostic run found no focused window and no renderer event in the original callback path. Native Undo and Redo selections were operated through CUA, with explicit polling of saved scene counts; native Command-Q exited the isolated app. The first Undo selection had no observed effect and the repeated selection worked; QA-CANVAS-002 remains open for that focus-transition uncertainty. Earlier asynchronous wait predicates gave unreliable timing evidence and have been replaced by explicitly awaited Node polling; prior callback-only passes are superseded by this run. The export dialog result remains injected, and Windows interaction is unverified. Credentials were not read or used.

Cross-platform keyboard checkpoint: run https://github.com/henryvn27/the-desk/actions/runs/34000816877 passed macOS/Windows checks/build/package at c775113e2923bd41e87fc2ad968300f92a3a30c5. The subsequent native-menu routing change is not covered by that run.

Canvas regression follow-up: installed macOS Canvas smoke passes with explicit image-import polling. A first run exposed a test baseline race: engine hydration normalized an empty binding list from null to [] before the deliberate stale-writer test. The regression now flushes hydration before recording the exact stored scene owned by the simulated other writer. Exact scene comparison after failed save and recovery is retained. Fixture teardown explicitly exits the isolated test app on failure, preventing the intentional save guard from concealing assertions. No production persistence behavior was weakened.

Cross-platform native-menu checkpoint: run https://github.com/henryvn27/the-desk/actions/runs/34001887438 passed macOS/Windows checks/build/package at f598626cf14e1a449373afd10c4487583dba3d88. Interactive Windows behavior and QA-CANVAS-002 remain open.

Notebook checkpoint: 45 tests, typecheck/lint/build, unsigned macOS packaging and installed notebook smoke pass. Library creates ordered portrait-page notebooks alongside infinite canvases. Fixed page frames clip rendering; current-page PNG export is exactly 794 by 1123 pixels. Installed checks cover two pages, shared math image retention, inactive-page preservation, current-page keyboard undo/redo, restart, and recovery of every page/image after a stale save without changing the original. Exported page and screenshots were visually inspected; operated videos captured. The exporter dialog result is injected. Schema 6 prevents older renderers from dropping page envelopes. The engine converter treated zero frame coordinates as absent; the adapter now explicitly restores Desk-owned origin/dimensions. A test comparison was moved after normal engine hydration before checking inactive-page preservation; exact comparisons remain. Undo history resets when changing pages. PDF annotation, page organization, multi-page PDF export, Windows interaction and full Canvas/V1 acceptance remain open. No provider/cloud credentials were accessed.

Notebook regression checkpoint: the installed schema-6 build also passes existing infinite-Canvas and math smoke, including images/fonts/source links, close protection/recovery, equation editing, and menu callbacks. These do not re-prove native menu selection focus or Windows runtime.


Plan commitment checkpoint: 47 tests, typecheck/lint/build, unsigned macOS packaging, development and installed Plan smoke pass. SQLite schema 7 adds stable, revision-checked study blocks. Form editing can reserve, move, resize and lock a block. Overlaps and stale edits fail transactionally; moving/unlocking locked blocks and scheduling beyond deadlines require explicit approval. Suggestions use gaps around all future saved blocks, subtract reservations once from remaining estimates, and retain a day-level capacity buffer. Elapsed blocks do not imply task completion. Installed proof exercises reserve, lock, denied edit, approved resize, and exact restart persistence; screenshot inspected and operated video captured. Drag/drop, cancellation, plan history, automatic rebalancing, broader planner intelligence and Windows interaction remain open.

User-directed phase change: Canvas/PDF work is paused. Unfinished PDF implementation is preserved only on local branch `codex/desk-pdf-wip` at `b07c518`; its typecheck failed and it was never installed. That branch has an independent experimental schema 7 and must be rebased with a new migration before any future integration. The current Plan build contains no PDF changes. Prior notebook checkpoint 655c02c passed hosted macOS/Windows CI run 34002999576.


Plan cancellation checkpoint: 48 tests, typecheck/lint/build, unsigned packaging, and development/installed macOS Plan smoke pass. Releasing reserved time requires explicit approval (with locked-specific wording), retains the block and cancellation timestamp, advances its revision, and does not modify assignment completion or remaining minutes. Released blocks no longer reserve capacity, cannot be edited back into active commitments, and remain visible under Released blocks after restart. New reservations can reuse released time. Schema 8 fences older clients that would mistake cancelled blocks for active commitments. Screenshot inspected and operated video captured. Full plan revision history, drag/drop, rebalance offers, broader planner intelligence and interactive Windows proof remain open. CI run 34003896496 passed macOS/Windows check/build/package at 1184c63.


Plan day-drag checkpoint: 48 tests plus typecheck/lint/build pass. Development and installed macOS Plan smoke operate a real drag between day columns, verify no persistence on drop, reject an unapproved locked move, and save the approved day change with its lock retained. Existing reserve/edit/cancel/restart checks also pass. Exact time editing remains form-based. Visual inspection caught whole-window overflow; the main grid now permits its content column to shrink and only the day strip scrolls horizontally. The final installed screenshot was inspected and smoke asserts no document-width overflow. Operated video captured. Automatic rebalance offers, full plan history, rich explanations and Windows interactive proof remain open. Prior cancellation CI 34004064060 passed macOS and Windows.


Plan rebalance checkpoint: 49 tests plus typecheck/lint/build pass; unsigned packaged and installed macOS Plan smoke passes preview, denied unapproved apply, approved apply preserving locked blocks and task work, and exact rebalance history after restart. Preview runs in the trusted store and holds a single opaque ID with a two-minute expiry and exact planning-state basis. Task, block, settings or session changes invalidate it. It starts proposals three minutes ahead, preserves locked/started/imminent blocks, and pauses rebalance while a study session is active. Apply atomically cancels replaced reservations, adds proposed blocks, stores the before/after change record and writes outbox intents. Required unscheduled work is shown and never deleted or marked complete. Schema 9 adds plan_changes; the UI shows the latest 50 rebalance records. Screenshot inspected and operated video captured. Existing reserve/drag/lock/cancel/restart regression passes. Automatic mode, full manual-edit history, stronger prioritization/spacing/learning and interactive Windows proof remain open. Prior day-drag CI 34004247455 passed macOS/Windows check/build/package.


Plan priority checkpoint: 52 tests plus typecheck/lint/build pass; final unsigned installed macOS Plan smoke creates assessment preparation with higher importance through the assignment form and verifies both choices after restart, alongside drag/lock/cancel/rebalance regression. Required work precedes optional review, same-window confirmed deadlines precede future high-importance work, and recorded importance/assessment type distinguish flexible work. Optional minutes do not inflate required overload, and explanations include recorded work type/importance. The first UI run timed out on exact select labels; explicit accessible names fixed the controls and the same flow passed. Form screenshot inspected and operated video captured. Schema 10 fences clients that would drop task metadata. This does not implement expected grade-impact ranking, grade weights, mastery, dependencies, spacing, learned estimates or automatic mode. Windows interaction remains unverified. Prior rebalance CI 34004486847 passed macOS/Windows check/build/package.


Manual gradebook checkpoint: 56 tests plus typecheck/lint/build, unsigned packaging, and development/installed macOS gradebook smoke pass. Class pages accept weighted categories and manual scores with revision-checked correction. Category totals above 100%, invalid points and cross-class/stale corrections are rejected transactionally. The model uses points within categories, distinguishes zero from missing scores, and leaves unscored/unconfigured weight unknown. Runtime verifies 80/20 categories, rejection at 101%, correction from 8/10 to 9/10, a 72–92% assumption-based range with 20% unscored weight, and exact records after restart. Screenshot inspected and operated video captured. Schema 11 adds grade_categories/grade_entries. Entries are user-entered, not official grades or teacher evidence. Screenshot extraction, drop/extra-credit rules, future-work projections, planner integration and Windows interaction remain unfinished. Prior priority CI 34004774417 passed both platforms.


Grade scenario checkpoint: 58 tests plus typecheck/lint/build, unsigned packaging, and installed macOS gradebook smoke pass. A hypothetical additional item accepts low/high earned points and possible points, recomputes category totals, and preserves uncertainty in other unscored weight. Reversed/out-of-bounds ranges are rejected; recorded entries remain exactly unchanged; scenario state is absent after restart. Existing category/score correction persistence passes. The first smoke rerun matched a hidden category option after the new selector was added; narrowing the test to the visible category row fixed the ambiguity without changing product validation. Screenshot inspected and operated video captured. These are explicit what-if assumptions, not predicted outcomes or official grades. Planner integration, screenshot extraction, grading exceptions and Windows interaction remain unfinished. Prior gradebook CI 34005085159 passed macOS/Windows checks/build/package.


Grade-context planning checkpoint: 62 tests plus typecheck/lint/build, unsigned packaging and installed macOS gradebook/planning smoke pass. Assignments can link a same-class category and upcoming possible points. Planning explains modeled sensitivity to a 10-percentage-point score change using recorded weights/points; this is potential influence, not predicted learning or score gain. Complete eligible required context enables sensitivity/days-to-deadline ordering after imminent deadlines and explicit importance; missing required context retains fallback ordering. Tests cover incomplete context, cross-class rejection, grade-change preview invalidation and earliest ordering among imminent deadlines. The latter fixes an edge case where later higher-importance work could unnecessarily crowd out an earlier deadline. Runtime links a 100-point item to the Tests category, observes 7.27 course percentage points of modeled sensitivity, and preserves link/grades through restart. Screenshot inspected and operated video captured. Schema 12 fences older clients dropping grade links. Expected study gains, learned estimates, automatic modes, mastery/spacing/teacher evidence and Windows interaction remain unfinished. Prior scenario CI 34005302262 passed both platforms.


Auto-plan new-work checkpoint: 64 tests plus typecheck/lint/build pass. Default Auto-plan captures reserve available time while preserving existing blocks; Suggest does not reserve. Mode persists, invalid stored modes fail closed, and uncertain/active-session captures stay proposals. Untouched automatic reservations can be removed with capture undo; edited/locked commitments prevent undo. Automatic additions create plan change records. Two manual block fixtures now explicitly select Suggest; dedicated default-mode tests cover automatic behavior. Development manual reserve/drag/lock/cancel/rebalance regression passes. Final unsigned installed Auto-plan smoke passes default capture, visible change record, mode switch without moving blocks, second Suggest capture and restart persistence. Screenshot inspected and operated video captured. Schema 13 fences origin/mode semantics. Automatic updates beyond new capture, deferred-session scheduling, Autopilot, external publishing and Windows interaction remain open. Prior grade-context CI 34005637130 passed both platforms. Bounded storage check: 16,854,072 KB available; ENV-001 remains open and no cleanup performed.


Deferred Auto-plan checkpoint: 66 tests plus typecheck/lint/build, unsigned packaging and installed macOS Auto-plan smoke pass. Captures during an active session retain pending intent across restart. Ending the session schedules them once while leaving existing commitments unchanged; switching to Suggest cancels pending intent, and manually reserved/cancelled tasks are not automatically duplicated. Pending status appears in the class list. Schema 14 fences pending intent. Runtime seeds the initial active session through the trusted test API, then performs capture, restart and session end through the UI and verifies the resulting reservation. Screenshot inspected and operated video captured. Broader replanning, automatic missed-block handling, Autopilot, external publishing and Windows interaction remain open. Prior Auto-plan CI 34006122393 passed both platforms.


Duration evidence checkpoint: 68 tests plus typecheck/lint/build, unsigned packaging and development/installed macOS duration smoke pass. SQLite schema 15 fences immutable start estimates and task revisions. Assignment edits and remaining-time corrections invalidate comparability without rewriting the baseline. Three reviewed, unchanged tasks completed in one session in the same class/work type enable an optional median actual/estimate adjustment. Legacy, partial, tiny, unreviewed and multiple-session work is excluded. UI application is explicit and remains unsaved until capture confirmation. Runtime seeds synthetic timing using real store commands, then operates capture, suggestion application, session start/end, remaining correction and restart; 60 minutes at start stays 60 while remaining work becomes 45. Screenshot inspected and operated video captured. A fixture initially omitted required studyDays; correcting the fixture resolved validation without changing the product schema. Time-of-day learning, multi-session estimates, broader session intelligence and Windows interaction remain unfinished. Previous deferred Auto-plan CI 34006442962 passed macOS/Windows checks/build/package. No provider/cloud integration or Canvas work was introduced.


Session correction checkpoint: 70 tests plus typecheck/lint/build, unsigned packaging and installed macOS correction/duration smoke pass. Schema 16 adds session revision and correction history. The latest ended session can be corrected from finished to unfinished or vice versa; reopening requires remaining minutes. Stale task/review revisions, active sessions and superseded sessions reject transactionally. Corrections retain actual time, timestamps, start estimate, prior completion report and prior review. Old sessions without completion evidence are displayed as unreported. Runtime operates capture/start/finish, reopens work, views history, injects a concurrent review to verify stale rejection, then corrects completion and restarts. Notes received an explicit accessible label after populated textarea text made its implicit label ambiguous; the test's concurrent-review wait was narrowed to the rendered paragraph. No validation was weakened. Final screenshot inspected and operated video captured. Duration regression still preserves the 60-minute start baseline after a 45-minute remaining correction. Automatic work detection, attempts, mistakes, mastery/preparedness updates and Windows interaction remain unfinished. Previous duration CI 34006977441 passed macOS/Windows checks/build/package.


Session Kit checkpoint: 72 tests plus typecheck/lint/build, unsigned packaging and installed macOS kit smoke pass. Preview and active session surfaces collect the assignment resource/notes, explicitly linked sources, separately labeled general class references, and the latest three nonblank ended-session reviews. Sources associated only with another task are excluded. Kit data reconstructs from authoritative SQLite across paused-session restart. Runtime performs capture, session end/review/start and source expansion; source associations and study-window settings are seeded through IPC. Trusted external-resource dispatch is stubbed to avoid altering the personal browser and is not live-navigation proof. Screenshot inspected and operated video captured. No schema migration, Canvas changes or provider/cloud integration. Structured checklist/progress, richer objectives, mistakes, calculator, generated artifacts and Windows interaction remain unfinished. Previous correction CI 34007362534 passed macOS/Windows checks/build/package.


Checklist checkpoint: 74 tests plus typecheck/lint/build, unsigned packaging and installed macOS checklist/Session Kit regression pass. Schema 17 adds task-owned checklist items and immutable session-end checklist snapshots. Items support add, check, edit, archive and restore with stable IDs and revision guards; completed tasks require reopening. Scope edits advance task revisions; checking does not. All checks do not automatically finish an assignment. Controller progress labels the next unchecked step rather than claiming observed activity. Its height is increased to 380 px with bounded title/progress text so the tested controls fit without scrolling. Runtime performs checklist CRUD/progress, pauses through the controller, restarts, ends/reviews and renames while preserving session history. Two test timing/locator corrections were needed: the edited row no longer contains its checkbox, and checked state is acknowledged asynchronously by SQLite/IPC. Tests now locate the editor directly and await persisted checkbox state. Screenshot/controller inspection and operated video captured. Structured attempts/mistakes/mastery, richer kit artifacts and Windows interaction remain unfinished. Previous Session Kit CI 34007577938 passed both hosted platforms. Physical internal free space was 13,191,840 KB; ENV-001 remains open, with no unrelated cleanup performed.


OpenRouter checkpoint: 77 tests plus typecheck/lint/build pass. Lens now uses main-process OpenRouter chat/completions with Desk-owned explicit model/provider routes, strict JSON output, required parameters, denied data collection, ZDR, and no provider fallback or retry. Text selects Terra; explicit image selects Gemini. Dated canonical identities are allowlisted, unknown substitutions rejected. Cost records actual OpenRouter-reported USD when present, otherwise null. Development-key loading is explicit and disabled for packaged builds. Native main-window file import encrypts a user key with safeStorage; raw-key renderer entry/IPC was removed and old provider key files are ignored/preserved. Development and unsigned installed synthetic smoke pass import, request, encrypted persistence, restart and disconnect; installed smoke also rejects Lens-window import. App archive excludes environment/credential files and installed archive matches packaged bytes. Settings screenshot inspected and operated video captured. Two bounded live requests using only synthetic data returned HTTP 401 authentication; no successful response, actual usage or privacy account settings verified. No further live retry until external credential/account state changes. Full V1 stays unfinished; Canvas/PDF remains paused. Prior checklist commit d770fec passed hosted macOS/Windows CI run 34008170710.


Capture Inbox checkpoint: 79 tests plus typecheck/lint/build pass. Schema 18 stores deterministic pasted-text interpretations with original batch text, source segments, capture instant/time zone, confidence and unresolved details. Closing a review leaves its pending record intact. No pending item becomes a task or reserves time. Atomic review checks the inbox revision, preserves trusted source evidence, creates one task and invokes existing planning policy; stale/duplicate reviews and invalid task data roll back. Archive/restore retains drafts, and safe task undo returns the capture to pending while removing its untouched automatic reservations. Development and unsigned installed Inbox smoke pass batch capture, source inspection, archive/restore, review, undo, stale-dialog rejection and restart. Final installed broader desktop regression passes Capture/Session/Review, deadline-change approval, settings and local Lens fallback. That older test initially assumed the default study window was open; after cutoff it correctly saw no Start button. Test fixtures now use an explicit UTC study window and future dates, with UTC expectations. Screenshot inspected, operated videos captured, installed/package app archives match. Capture automation modes and high-confidence filing, non-text intake, source conflicts and Windows interaction remain open. Canvas/PDF stays paused. Prior OpenRouter commit ee1dcb3 passed hosted macOS/Windows CI run 34009382513.


Capture confidence checkpoint: 82 tests plus typecheck/lint/build pass. Schema 19 persists Conservative/Balanced-default/Autopilot capture modes and records each new draft's decision. Complete literal high-confidence text can file automatically; Autopilot also accepts a unique partial class-name match. Missing/ambiguous fields, assessment preparation, unsupported values, multiple resources and same-class/title duplicates stay for review. No mode retroactively accepts pending work or changes existing deadlines. Shared task creation preserves existing Auto-plan/deferred-session behavior. Filed history exposes why and supports safe undo; edited assignments cannot be erased by undo. Development and unsigned installed policy smoke pass mode selection, automatic filing, uncertainty routing, reason visibility, undo and restart; installed Inbox regression also passes. Screenshot inspected and operated video captured. Installed/package app archives match. Non-text intake, wider authority/conflict resolution and Windows interactive proof remain open. Canvas/PDF remains paused; OpenRouter HTTP 401 remains external and no request was retried. Prior Inbox commit d6502fa passed hosted macOS/Windows CI run 34009839765.


Text-file intake checkpoint: 85 tests plus typecheck/lint/build pass. Native Capture import accepts up to ten UTF-8 .txt/.md files through the main-window picker, with bounded bytes/characters and rejection of unsupported/binary/invalid/recognized credential-bearing content. The renderer supplies no path. Failed files or excessive draft batches save nothing. Each file retains its basename and decoded source text, independently interpreted under existing capture policies; accepted tasks preserve file provenance. Development and unsigned installed smoke pass cancellation, mixed invalid-batch recovery, multi-file import, source inspection, review, Lens-window import denial and restart. Automated native selections use synthetic files and a dialog stub; this does not prove manual OS-picker interaction or Windows UI. Screenshot inspected and operated video captured; installed/package app archives match. Rich documents, PDF/OCR/images, browser, email, voice and drag/drop intake remain open. Canvas/PDF remains paused. Prior capture-policy commit 4b014d1 passed hosted macOS/Windows CI run 34010231688. No provider requests or credential-file changes occurred.


Native-picker proof checkpoint (installed code 0c464c8): launched the unsigned app with isolated DESK_DATA_DIR and a synthetic native-physics-notes.md file; no developer-key loading or native-dialog stub. CUA clicked Import text files, navigated the real macOS open panel to the exact fixture through Go to Folder, selected it, and clicked Import. The resulting live UI showed one pending item and its filename. The verification harness asserted one SQLite Inbox entry with text-file source type/correct basename and zero tasks, captured the result, then closed the app and removed its owned temporary workspace. Native sheet screenshot and app-result screenshot retained; the Playwright video captures the app surface, not the OS sheet interaction. The real macOS-picker gap from the prior checkpoint is closed. Windows native interaction remains unverified. Hosted macOS/Windows CI run 34010710668 passed for 0c464c8. No app source change or rebuild was required for this evidence checkpoint.


Tutoring-mode checkpoint: 88 tests plus typecheck/lint/build pass. Schema 21 persists Guide me, Balanced-default and Explain directly modes. Main-process request construction uses the saved setting, rejects renderer overrides, preserves explicit full-method assistance, and rejects provider action payloads. Development and unsigned installed synthetic smoke pass selection, SQLite persistence, request instructions, action rejection, zero task creation and restart. The smoke exposed an internal LensProviderError prefix; shared user-error formatting was fixed and the exact friendly-message assertion passes. Installed/package app archives match; screenshot inspected and operated video retained. Responses are synthetic: class-material grounding, live teaching quality and Windows interaction remain unverified. OpenRouter HTTP 401 was not retried. Full V1 remains unfinished and Canvas/PDF remains paused. Prior native-picker checkpoint dd6c97b passed hosted macOS/Windows CI run 34011036049.


Local-grounding checkpoint: 90 tests plus typecheck/lint/build pass. Trusted-main Lens context includes active-task sources before class-wide sources with no other task links. It preserves user-provided authority, excerpt truncation and omitted counts under a serialized 20,000-character budget. No URL fetch or inferred teacher authority. Development and unsigned installed synthetic smoke prove task-linked inclusion and unrelated exclusion in the outbound request, tutoring modes, action rejection and restart. An initial fixture incorrectly parsed the first user history turn as structured current input; selecting the current content-array message fixed the fixture without relaxing assertions. Installed/package archives match; screenshot inspected and operated video retained. Association-based excerpts do not prove relevance retrieval, citations, source fidelity in live answers or teaching quality. OpenRouter 401 remains external and was not retried. Full V1 remains active; Canvas/PDF paused. Prior tutoring commit a516565 passed hosted macOS/Windows CI run 34011697764.


Source-priority checkpoint: 92 tests plus typecheck/lint/build pass. Schema 22 retains legacy source text/links/ingestion authority while adding unspecified kind and revision zero; an existing-source schema-21 migration test passes. User-reported class material, assigned textbook, educational reference and general web types can be selected on creation and corrected afterward with stale-revision rejection. Lens orders eligible sources by this hierarchy, then task association, retaining kindReportedBy=user. Development and unsigned installed synthetic UI smoke pass source creation, correction, request order/provenance, exclusion of unrelated sources, tutoring modes/action rejection and restart. The first UI run could not resolve the new selector by an exact accessible label; an explicit label fixed the control and the same assertion passes. Installed/package archives match; screenshot inspected and operated video captured. No live teaching/source-fidelity or Windows UI claim. Full V1 remains active, Canvas/PDF paused, OpenRouter 401 not retried. Prior grounding commit 256ec1f passed hosted macOS/Windows CI 34011923767.


Passage-retrieval checkpoint: 95 tests pass, with typecheck/lint/build passing for the implementation. Validated current questions select matching 3,000-character original-text windows, scored by distinct lexical terms. Exact UTF-16 offsets, truncation and opening-fallback metadata disclose the selection. Source hierarchy remains first; lexical coverage precedes association within one type. Scope exclusion and serialized context bounds are preserved. Development and unsigned installed synthetic smoke verify a matching passage beyond the original opening, exact substring offsets, unrelated exclusion, mode persistence/action rejection and restart. Installed/package app archives match. No new visible UI was introduced. Semantic relevance, citations and real answer fidelity remain unverified; no live provider request was retried. Full V1 remains active and Canvas/PDF paused. Prior source-priority commit 9553587 passed hosted macOS/Windows CI 34012192327.


Explicit-memory checkpoint: 97 tests plus typecheck/lint/build pass. Schema 23 stores explicit memory notes with category/class scope, provenance, revisions and timestamps. What The Desk Knows supports create/edit/forget; stale corrections and invalid classes fail atomically. Notes do not alter planner settings/tasks. Active-session Lens receives bounded global/current-class statements with explicit provenance and omitted count. Development and unsigned installed UI smoke pass create/edit/restart/forget/forgotten-state restart. The initial edit check failed because a populated textarea lacked a stable accessible name; rendered form evidence isolated the issue, an explicit label fixed it, and the original assertions pass. Installed/package archives match; screenshot inspected and operated video retained. No provider calls occurred. Behavioral inference/confirmation, disable/clear-inferred controls and integration of existing learned preferences remain open, as does live tutoring use and Windows interaction. Full V1 active; Canvas/PDF paused; OpenRouter 401 not retried. Prior c88280f hosted macOS/Windows CI 34012358454 passed.


Inferred-duration memory checkpoint: 99 tests plus typecheck/lint/build pass. Schema 24 persists learning controls and excluded evidence. Three qualifying reviewed tasks produce a candidate only; trusted-store confirmation validates its current basis and saves median ratio/sample/session provenance. Disable stops Capture duration learning and inferred tutoring notes. Clear removes inferred notes, preserves explicit notes and study history, and prevents old evidence from recreating suggestions, including partial task histories. Editing retains inference provenance. Development and unsigned installed UI smoke pass candidate review/confirmation, toggling, explicit editing, clear preservation and restart; installed smoke additionally verifies disabled preference after restart. Synthetic clock-injected sessions are fixture data, not human study evidence. The first UI run exposed an async checkbox reversion; immediate local state with rollback fixed it and original checks pass. Installed/package archives match; screenshot inspected and operated video retained. Broader preference inference and existing-setting memory integration remain open. No provider calls. Full V1 active; Canvas/PDF paused. Prior c7b12d9 passed hosted macOS/Windows CI 34012668967.


Memory-evidence checkpoint: 100 tests pass; implementation typecheck/lint/build pass. Schema 25 captures original task/session revisions, labels, scope/type, actual/estimated minutes and ratio when confirming duration memory. Later review correction preserves those observations. Evidence UI uses saved values and warns when source history changed or legacy observations are unavailable. Such inferred notes are withheld from tutoring; explicit notes remain available. Development and unsigned installed synthetic UI smoke pass confirmation, source-review correction, immutable evidence assertion, visible warning, existing memory controls and restart. Installed/package archives match; screenshot inspected and operated video retained. No provider requests or real human-duration claims. Broader preference learning/settings integration and live tutoring/Windows UI remain open; full V1 active, Canvas/PDF paused. Prior d8a6a2f hosted macOS/Windows CI 34012974486 passed.
Mistakes checkpoint (commit 01b0789): 103 tests plus typecheck/lint/build pass. Schema 26 adds durable, revision-checked Mistake records with concept, source, original attempt, what went wrong, correction, help used, confidence, review due and linked practice-task IDs. The trusted store validates class/task ownership, preserves the record beyond its assignment, generates an optional-review practice task, and makes linked or due mistakes visible in deterministic plan explanations. The Mistakes view records and displays every field, supports edit/forget, and keeps generated practice when the mistake is forgotten. Session Kit includes same-class mistakes as read-only context. Development and unsigned installed arm64 smoke pass create/edit/practice/restart/forget, assert all fields and revision changes, and confirm no renderer errors. Screenshot inspected and operated video retained at `artifacts/mistakes/`; installed `app.asar` matches the package. Hosted macOS/Windows checks/build/package pass at 34014144659. Windows UI interaction, broader attempts/mastery/preparedness graph work, and live tutoring remain open. Canvas/PDF remains paused; no provider request was made.
Concepts/preparedness checkpoint (commit ccaa45a): 106 tests plus typecheck/lint/build pass. Schema 27 adds durable, revision-checked Concept records that survive individual assignments, link many same-class tasks, and retain explicit status, preparedness, course/long-term retention, review due, attempts, unaided accuracy, hint count, recency and evidence notes. Duplicate class-local names, cross-class task links and impossible unaided totals fail at the trusted boundary. Linked, weak or due concepts influence deterministic planner explanations and appear in Session Kit as read-only context. Development and unsigned installed arm64 smoke pass create/edit/restart/forget, assert the evidence and revision changes, and confirm no renderer errors; installed `app.asar` matches the packaged artifact. Screenshot inspected and operated video retained at `artifacts/concepts/`. Hosted macOS/Windows checks/build/package run 34015001662. Windows UI interaction, cumulative recall, automatic preparedness inference, final-exam retention scheduling and live tutoring remain open. Canvas/PDF remains paused; no provider request was made.

Attempts evidence checkpoint (commit 8ebbfd1): 108 tests plus typecheck/lint/build pass. Schema 28 adds durable, revision-checked Attempt records with explicit correct/incorrect/partial/unknown result, unaided/aided state, bounded hints, notes, optional same-class task and many same-class concept links. Create/edit/forget updates linked concept attempt, unaided and hint counters atomically and reversibly; stale writes, cross-class links and aided zero-hint records are rejected. The Attempts view is inspectable and correction-safe, and Session Kit exposes the five most recent assignment-linked attempts as read-only evidence. Development and unsigned installed arm64 smoke pass record/edit/restart/forget the attempt, verify linked concept counters and preserved task state, and confirm no renderer errors; installed `app.asar` matches the packaged artifact. Screenshot inspected and operated video retained at `artifacts/attempts/` and the user-facing outputs folder. Hosted macOS/Windows checks/build/package pass at 34015589075. Automatic mastery/preparedness transitions, cumulative recall/final-exam scheduling, broader academic graph work, Windows UI interaction and live tutoring remain open. Canvas/PDF remains paused; OpenRouter 401 remains an external blocker and was not retried.

Assessments checkpoint (commit b314185): 111 tests plus typecheck/lint/build pass. Schema 29 adds durable, revision-checked Assessment records with explicit quiz/test/exam/final/midterm/project/essay/lab/presentation/standardized-test/other types, optional due dates and grade-category links, many same-class preparation-task links, and notes. Create/edit/forget rejects stale revisions, duplicate class-local titles, invalid dates and cross-class task/category links. Assessment-linked work receives deterministic planner priority and Session Kit shows the linked context. Development and unsigned installed arm64 smoke pass create/edit/restart/forget, preserve the preparation task and grade category, confirm no renderer errors, and match installed/package `app.asar` bytes. Screenshot inspected and operated video retained at `artifacts/assessments/` and the user-facing outputs folder; Attempts, broad desktop and checklist/session installed regressions also pass. Hosted macOS/Windows checks/build/package pass at 34016570670. Photo/OCR/graded-work extraction, teacher evidence and score/question/comment/rubric ingestion, automatic mastery/preparedness, cumulative recall/final-exam scheduling, broader academic graph work, Windows UI interaction, live tutoring and sync/account integrations remain open. Canvas/PDF remains paused; OpenRouter 401 remains an external blocker and was not retried.

Teacher evidence checkpoint (commit 13aa453): 114 tests plus typecheck/lint/build pass. Schema 30 adds durable, revision-checked teacher-reported evidence linked to classes, assessments, tasks and concepts. Manual score pairs, teacher comments, rubrics, observed questions/markings/patterns, source labels and explicit inclusion/exclusion from future teacher-pattern review remain distinct from Desk inference. Development and unsigned installed arm64 smoke pass create/edit/restart/forget, preserve linked assessment/task/concept records, confirm no renderer errors, and match installed/package `app.asar` bytes. Screenshot inspected and operated video retained at `artifacts/evidence/` and the user-facing outputs folder; assessment, Attempts, broad desktop and checklist/session installed regressions also pass. Hosted macOS/Windows checks/build/package pass at 34017304936. Image/OCR extraction, durable Teacher identity, inferred teacher patterns, generated practice from observed structure, score/question/rubric extraction, broader academic graph work, Windows UI interaction, live tutoring and sync/account integrations remain open. Canvas/PDF remains paused; OpenRouter 401 remains an external blocker and was not retried.

Authority conflict checkpoint (commit f59e9a3): 117 tests plus typecheck/lint/build pass. Schema 31 adds revision-safe local `authority_claims` and explicit `authority_resolutions` for due-date provenance. Claims retain the reported value, contextual authority class, confidence, source label, details, capture time and optional source/teacher-evidence links. The Authority view preserves competing reports, orders them contextually, and requires an explicit student choice before changing an assignment deadline; Lens receives the claims and resolution state so it can ask instead of silently selecting. Development and unsigned installed arm64 smoke create syllabus and live-LMS claims, show the conflict, apply one choice, restart and preserve both claims plus the resolution; the teacher-evidence, assessment, Attempts, broad desktop and checklist regressions also pass. Screenshot and operated video are retained at `artifacts/authority/` and the user-facing outputs folder; installed `app.asar` matches the package. Hosted macOS/Windows checks/build/package pass at 34018079768. Live integrations, automatic conflict generation, broader fact types, durable Teacher identity/patterns, Windows UI interaction, sync/account integrations and the OpenRouter 401 remain open. Canvas/PDF remains paused.


Teacher identity checkpoint (commit ef28d8c): 120 tests plus typecheck/lint/build pass. Schema 32 adds durable local `teachers` with `teacher_classes` many-to-many class links, revision-safe identity CRUD, duplicate-name protection within overlapping classes, explicit email/notes, and safe removal guards while teacher evidence still targets the identity. Teacher evidence can be linked or explicitly unlinked from a same-class Teacher; Teachers, Evidence and Session Kit keep explicit identity separate from Desk-inferred patterns. Development and unsigned installed arm64 smoke creates a Teacher across two classes, links teacher evidence, restarts, blocks unsafe identity removal, exercises unlink/forget, and confirms no renderer errors; installed evidence, authority, assessment, Attempts, broad desktop and checklist regressions also pass. Screenshot and operated video are retained at `artifacts/teachers/` and the user-facing outputs folder; installed `app.asar` matches the package. Hosted macOS/Windows checks/build/package pass at 34019137960. Teacher-pattern inference, roster sync, graded-work/image/OCR extraction, Windows UI interaction, sync/account integrations and the OpenRouter 401 remain open. Canvas/PDF remains paused.

Track and Unit hierarchy checkpoint (commit 5a0f54b): 123 tests plus typecheck/lint/build pass. Schema 33 adds durable local `tracks` and `units`/modules with class-scoped ownership, optional same-class Track links, explicit sequence/kind/notes, many-to-many task links, duplicate-name and revision checks, cross-class rejection, and safe guards for linked task/Track changes and undo. The Units view creates and maintains the hierarchy, exposes linked task counts and nested unit controls, and keeps planning conservative. Session Kit includes only the active task's explicit Units and Tracks. Development and unsigned installed arm64 smoke creates a Track, links a Unit and task, restarts, blocks unsafe Track removal, maintains the linked Unit from the nested view, forgets safely, and confirms no renderer errors. Screenshot and operated video are retained at `artifacts/units/` and the user-facing outputs folder; installed `app.asar` matches the package. Installed Teacher, evidence, assessment, Attempts, Authority, desktop, checklist and Session Kit regressions also pass. Hosted macOS/Windows checks/build/package pass at 34020056285. User identity, academic periods, persistent plan versions, integrations, connection capabilities, prerequisites, hierarchy-aware planner behavior, Windows UI interaction, sync/account integrations and the OpenRouter 401 remain open. Canvas/PDF remains paused.

Academic context checkpoint (commit 36eb036): 126 tests plus typecheck/lint/build pass. Schema 34 adds durable local `academic_periods` and `spaces` with explicit many-to-many class links, typed period/space kinds, optional period date ranges, notes, duplicate-name and unknown-class validation, revision-safe CRUD, restart persistence, and safe forget guards that require explicit unlinking. The Academic context view creates and maintains both records; Session Kit filters matching period and space context into the active class without changing planner ordering. Development and unsigned installed arm64 smoke creates Fall 2026 and school-space records across classes, restarts, blocks unsafe removal while linked, unlinks explicitly, forgets safely, and confirms no renderer errors. Screenshot and operated video are retained at `artifacts/context/` and the user-facing outputs folder; installed `app.asar` matches the package. Installed Units, Teachers, evidence, assessments, Attempts, Authority, desktop, checklist and Session Kit regressions also pass. Hosted macOS/Windows checks/build/package pass at 34020858817. User identity, persistent plan versions, integrations, connection capabilities, prerequisites, hierarchy-aware planner behavior, Windows UI interaction, sync/account integrations and the OpenRouter 401 remain open. Canvas/PDF remains paused.

Local User identity checkpoint (commit b187b7d): 128 tests plus typecheck/lint/build pass. Schema 35 adds one durable local `users` profile per SQLite store with explicit display name, optional email, time zone, user-entered authority, revision-safe create/edit/forget and restart persistence. Settings exposes the profile and clearly separates local identity from account login and school connections; AI telemetry uses the profile ID when present. Development and unsigned installed arm64 smoke creates the profile, restarts, updates by revision, forgets safely without touching a class, and confirms no renderer errors. Screenshot and operated video are retained at `artifacts/user/` and the user-facing outputs folder; installed `app.asar` matches the package. Installed Academic context, Units, Teachers, evidence, assessments, Attempts, Authority, desktop, checklist and Session Kit regressions also pass. Hosted macOS/Windows checks/build/package pass at 34043522745. Account/login auth, persistent plan versions, integrations, connection capabilities, prerequisites, hierarchy-aware planner behavior, Windows UI interaction, sync/account integrations and the OpenRouter 401 remain open. Canvas/PDF remains paused.
